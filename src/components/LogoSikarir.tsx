import React from 'react';

export default function LogoSikarir({ className = "" }: { className?: string }) {
  return (
    <div className={`flex items-center gap-4 ${className}`}>
      {/* ... (isi kode SVG Sikarir yang tadi) ... */}
    </div>
  );
}